

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env` and set your `OLLAMA_API_KEY` (create a free key at https://ollama.com/settings/keys)
3. Run the app:
   `npm run dev`
